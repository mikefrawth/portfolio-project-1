# Core API framework - FastAPI is a modern, fast Python web framework
fastapi==0.111.0

# Mangum is the adapter that lets FastAPI run inside AWS Lambda
# Without this, Lambda wouldn't know how to talk to FastAPI
mangum==0.17.0

# ASGI server for running FastAPI locally during development
# Not used in Lambda - only for `uvicorn app.main:app` locally
uvicorn==0.29.0

# Pydantic is used by FastAPI for data validation and serialization
# It ensures API inputs/outputs match the shapes we define in models/
pydantic==2.7.1

# PyYAML lets us parse CloudFormation templates written in YAML format
# CloudFormation supports both JSON and YAML - we need to handle both
PyYAML==6.0.1

# AWS SDK for Python - used to interact with DynamoDB for saving diagrams
boto3==1.34.84

# UUID generation for creating unique diagram IDs
# (built into Python stdlib, listed here for clarity)
# uuid - no install needed
